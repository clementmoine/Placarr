Dockerfile
.dockerignore
node_modules
npm-debug.log
README.md
.next
.git

# Données runtime — vivent dans des volumes / bind mounts, jamais dans l'image.
cache
data

# Secrets — l'environnement est fourni au runtime par compose.
.env
.env.*

# Dev uniquement.
scratch
coverage
tests/fixtures
compose.yml
compose.dev.yml
